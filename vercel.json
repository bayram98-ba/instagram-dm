{
  "buildCommand": "sed -i 's/provider = \"sqlite\"/provider = \"postgresql\"/' prisma/schema.prisma && sed -i 's/provider = \"sqlite\"/provider = \"postgresql\"/' prisma/migrations/migration_lock.toml && npx prisma generate && npx prisma migrate deploy && next build",
  "framework": "nextjs"
}
